project Default is

   for Main use ("prj.adb", "prj.adb");

   package Compiler is
      for Default_Switches ("ada") use ("-g", "-O2");
   end Compiler;

   for Source_Dirs use ("./**");

end Default;

